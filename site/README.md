# Enqiu site adapter

The editable landing page lives at `../docs/index.html`.

This small deployment project copies that page and its social card into the
Sites build. Run `npm run dev`, `npm run build`, or `npm test`; each command
synchronizes the upper landing page first.
